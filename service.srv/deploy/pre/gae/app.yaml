runtime: nodejs22
env: standard
service: srv

# Backend NestJS — ambiente PRE
# No deploy o GAE corre: npm install && npm run build (nest build -> dist/)
entrypoint: npm run start:prod

env_variables:
  NODE_ENV: "production"
  APP_ENV: "pre"
